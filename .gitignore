# Credentials and local configuration.
.env
.env.*
!.env.example
*.key
secrets/
deploy/.dbpass
deploy/*.dbpass

# Source data and database materializations that are distributed separately.
data/snapshot/
data/db/
*.parquet

# Local R state.
.Rhistory
.RData
.Rproj.user/

# Python caches and local implementation evidence.
__pycache__/
*.py[cod]
.omo/
.gstack/

# Generated document and build output.
build/
logs/
*.tmp
.DS_Store

# Harvest bookkeeping regenerated by R/harvest_frame.R.
data/frame/partitions_done.txt
data/frame/s3_listing.txt

# Compact classifier release inputs remain tracked. Cross validation working
# files and full frame predictions are distributed as release assets.
!artifacts/frame_classifier/metadata.json
!artifacts/frame_classifier/model.joblib
artifacts/frame_classifier/folds.parquet
artifacts/frame_classifier/oof_predictions.parquet

# Large screening transcripts are distributed separately. Consolidated labels
# and provenance hashes remain in Git.
pilot/screening/loop/round_*/prompts/
pilot/screening/loop/round_*/raw_*/
pilot/screening/loop/round_*/quarantine/
